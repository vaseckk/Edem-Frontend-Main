// // Import vendor jQuery plugin example
// import '~/app/libs/mmenu/dist/mmenu.js'

document.addEventListener('DOMContentLoaded', () => {

	// Custom JS

})

function showAdditionalCircle() {
	const additionalCircle = document.querySelector('.additional-circle');
	additionalCircle.classList.toggle('hidden');
  }




/*const agreeButton = document.getElementById('agreeButton');
const agreementCheckbox = document.getElementById('agreementCheckbox');

agreeButton.addEventListener('click', function() {
    if (agreementCheckbox.style.display === 'none') {
        agreementCheckbox.style.display = 'inline-block';  // Показываем галочку
    } else {
        agreementCheckbox.style.display = 'none';  // Скрываем галочку
    }
});*/